npm i express cors node - instala o express com o método cors(middleware que diz ao seu servidor para permitir que sites específicos conversem com a sua API,
ja que os navegadores bloqueiam requisições de sites desconhecidos por segurança. 
